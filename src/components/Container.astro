---
interface Props {
    tailwindClass?: string;
}
const { tailwindClass = "" } = Astro.props;
---


<div
style={{
    backgroundColor: "var(--light)"
}}
class={"w-full h-full p-5 text-secondary rounded-md flex flex-col gap-2 " + tailwindClass}
>
    <slot />
</div>